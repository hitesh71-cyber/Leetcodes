class Solution {
public:
    int subarraySum(vector<int>& nums, int k) {
        int add=0;
        int c=0;
        for(int i=0;i<nums.size();i++)
        {
            add=0;
            for(int j=i;j<nums.size();j++)
            {
                add+=nums[j];
                if(add==k)
                {c++;
                }
            }
            
        }
        return c;
    }
};
